# D0.3 · RuntimeAdapter 交付状态

状态：**PASS — INDEPENDENT REVIEW COMPLETE**
分支：`refactor/phase-desktop-task-d0-3`
范围：共享 TypeScript 契约、Web/Fake 实现、浏览器能力完整接管；未建立正式 Tauri/Rust。

## 四问

1. **读什么**：AI 仍只经 `CONTEXT_SOURCES/assembleContext` 读取；RuntimeAdapter
   只传输共享 TS 已组装的 body 和原始响应字节。
2. **写什么**：AI 结果仍经 `FIELD_REGISTRY + AdoptionSchema/adopt` 写回；
   RuntimeAdapter 不写任何业务字段。
3. **涉及哪些表生命周期**：导出、备份和后续迁移 DTO 仍由
   `PROJECT_TABLES` 派生；RuntimeAdapter 只移动文本/字节，不维护表清单。
4. **注册表是否缺登记**：本任务未新增表、字段、上下文源或 AI 动作，三个
   注册表无需且没有修改。

## 已落地

- `RuntimeAdapter` 覆盖 ai、gist、files/backup binding、secrets、clipboard、
  external、durability、distribution、updates、diagnostics。
- AI 只允许 typed chat/embedding POST；精确保留用户 `configuredBaseUrl`。为
  保持现有 Web 配置零丢失，Web 允许登记的 Vite proxy alias 与已配置的
  HTTP/HTTPS（含 LAN），拒绝未知 alias、路径穿越、userinfo、非 HTTP(S) 和
  协议相对地址；Native 自定义源必须使用 `approvalId` 并在 D1/D3 实施更严格
  私网策略。Prompt/body、SSE、retry、usage、adopt 全部留在共享 TS。
- AI/Gist/raw fetch 统一 `redirect: 'error'`；raw 只允许无 userinfo、默认端口的
  精确 `https://gist.githubusercontent.com` origin。
- SecretStore 只有 `put/has/reference/delete`；明文只进入 `put`，网络能力只接收
  opaque `CredentialId`，没有 plaintext `get`。
- open/save/backup 使用互不扩权的 purpose union；adapter 内固定 accept、MIME
  与扩展名。source document 包含 TXT/MD/CSV/PDF/DOCX，reference document
  包含 TXT/MD/EPUB，模板库使用独立 `prompt-library-json`。无 FSA 文件选择器时
  Web 回退到固定 accept 的 `<input type="file">`，并复核实际扩展名。
- AI 响应流逐 chunk 检查 AbortSignal；消费者提前结束时 best-effort cancel
  reader。目录备份在关键 await 后检查中止，写入名必须与读取规则一致，并保留
  write 主错误，不被 close 二次错误覆盖。
- diagnostics 是固定 discriminated union；clipboard/external 也是固定用途，
  不接受任意 code、字段或 URL。
- Fake 覆盖成功、用户取消、AbortSignal、timeout、permission denied、disk full。
- AI stream/chat、连接测试与 embedding 已接管，SSE、UTF-8 跨 chunk、thinking、
  retry/backoff、timeout、Abort、usage 与错误语义仍由共享 TypeScript 保持。
- Web 已接管 Gist 全链路、当前全部导入导出、危险操作前备份、PNG 字节保存、
  ImportDoc、目录绑定/自动备份/恢复、两处剪贴板和两个外链目的地。
- 动态文件名统一净化；首次绑定目录后下一轮 timer 即可自动备份；文件打开/解析
  失败不会残留可继续处理的旧 ImportDoc 派生状态，用户取消仍静默且保留旧选择。
- CredentialId 与写入时的精确 secret value 绑定；轮换不会让旧请求误用新值；
  匿名请求不再错误撤销另一个在途请求，显式 `delete` 仍会使该 logical key 的
  所有引用失效。
- CredentialId 同时精确绑定 capability、provider profile、operation 与 configured origin；
  Gist/AI、不同 profile 和不同 origin 之间不能混用。清空密钥、取消记住、切换 provider、
  应用或删除 preset 都会同步清理对应 vault 引用，不会把旧 provider 密钥带入新配置。
- source document 在分配 `ArrayBuffer` 前执行共享大小限制；fallback 文件选择器给 focus/change
  保留安全时序窗口。目录备份写失败会 abort 而非提交半文件，恢复按文件懒加载，单个不可读
  候选会跳过且不会累计整个目录的原始字节。
- durability、Web Service Worker/update 生命周期和 distribution metadata 已接管。
- 架构守卫禁止业务层直接使用 fetch、File System Access、Blob 下载 anchor、
  file input、clipboard/storage/Service Worker、`window.open`/`_blank`，并禁止
  `src/runtime/tauri` 之外导入 Tauri API 或读取 `window.__TAURI__`。
- 明确拒绝复用 `tmp/tauri-poc` 的任意 URL `llm_proxy`。

## 状态边界

代码级完成判据已经满足：所有已盘点 browser-only 调用都有唯一 owner，业务目录
直接调用为零，且不存在万能 IPC 或散落 target 判断。D0.3 已通过独立审查并标记 PASS；
该 PASS 只覆盖共享运行时边界，不扩大到以下后续范围：

- 正式 Tauri/Rust、Windows Credential Manager、原生文件系统、签名更新、打包
  smoke 与真实数据迁移属于 D1–D4，不得用本任务状态冒充“客户端已可用”；
- D0.4 的真实浏览器/PWA 基线仍需得到浏览器控制授权后执行。

Tauri target 在显式注册 native adapter 前继续 fail closed，不会暗中退回 Web 实现。

## 独立审查记录（2026-07-14）

- 结论：**APPROVE**；稳定树无 P0/P1，也无 D0.3 新增的 P2 blocker。
- 独立复跑：architecture guard 17 + Web runtime 41 + Gist 9，共 67/67 PASS。
- 已确认：凭据作用域/生命周期、长项目名 Gist、备份原子写与懒恢复、取消语义、
  外链、文件大小预检、TypeError 分类，以及 fetch alias/location/target 分支守卫。
- 剩余仅 P3 一致性/守卫精度项；正式 Credential Manager、native timeout/single-flight
  与真实格式/客户端 smoke 按 D1–D4 跟踪，不改变 D0.3 结论。

## 验证证据（2026-07-14）

| 命令 | 结果 |
| --- | --- |
| D0.3 定向 `npm test -- --typecheck ...` | PASS；11 files / 109 tests，Type Errors 0；覆盖 contract/Web/Fake、AI、Gist、文件、目录备份、PWA 分发、配置兼容与架构守卫反例 |
| `npx tsc --noEmit` | PASS |
| `npm run check:architecture` | PASS |
| `npm run check:required-tables` | PASS；42 tables |
| `npm run build` | PASS；3426 modules transformed，PWA precache 106 entries |
| `npm run lint` | PASS（0 errors）；仓库基线 34 warnings，D0.3 文件无新增 warning |
| `npm test -- --typecheck` | 444/445 tests PASS（104/105 test files），Type Errors 0；唯一失败为既有 AI manual 漂移 |
| `npm run check:ai-manual` | FAIL；生成文件未包含既有 `character.dimension` 两个变量和调用 category，与 D0.3 无关 |

AI manual 漂移只读比对显示：`character.dimension` 已有代码事实新增
`generationHint` / `fieldFormat`，category 从 44 变 45，并新增
`CharacterDimensionFields.tsx` 调用记录；本分支没有修改这些 AI 业务事实，因此未把
无关生成文件混入 D0.3。

本任务没有 Rust/Tauri、IPC、capability 或 packaging 改动，故未运行 Cargo、Windows
客户端或 WebView2 smoke；没有启动或遗留任何长驻进程。真实浏览器/PWA smoke
受仓库低内存规则约束，未在未授权情况下启动。
