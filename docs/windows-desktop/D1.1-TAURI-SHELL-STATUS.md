# D1.1 · Tauri 2 壳交付状态

状态：**IN PROGRESS — 受限实现已落地，最终 PASS 等待 D0 总门**

分支：`refactor/phase-desktop-task-d1-1`

日期：2026-07-15

范围：仅开发隔离身份、空/合成数据和最小 capability；没有连接真实密钥、正式 profile 或真实用户数据。

## 本次结论

D1.1 已从“只有规格”推进到可重复构建、可离线启动的真实 Windows Tauri 2 壳。当前 release exe 在没有 Vite/Node 服务时从 `http://tauri.localhost/` 加载内嵌前端，首页和首次引导已在真实 WebView2 中渲染。

这仍不是作者自用候选或功能等价结论。D0.2、D0.4、D0.5 尚未全部闭环，D1.2～D1.5/G1、迁移、签名和发布也未完成，因此 D1.1 继续保持 `IN PROGRESS`，真实数据仍禁止进入。

## 四问

1. **读什么**：本任务只读取构建配置、RuntimeAdapter 契约和页面启动状态；没有新增 AI 上下文源，也没有绕过 `CONTEXT_SOURCES/assembleContext`。
2. **写什么**：只写桌面壳、构建产物和运行时诊断；没有新增 AI 业务写入，也没有绕过 `FIELD_REGISTRY + AdoptionSchema/adopt`。
3. **涉及哪些表生命周期**：没有修改 Dexie schema、迁移或表清单；`PROJECT_TABLES` 仍是项目表生命周期唯一事实源。
4. **注册表是否缺登记**：没有新增表、字段、AI source 或 AI action，三个注册表无需修改。

## 已落地

- 新建正式 `src-tauri/`，固定 `tauri 2.11.5`、`tauri-build 2.6.3`，`main.rs` 只调用 `storyforge_desktop_lib::run()`。
- 默认启用 `custom-protocol`，避免 release exe 错误回连开发地址。
- 建立开发/正式两套 identity 配置；本次只运行 `io.github.yuanbw2025.storyforge.dev`，实际 UDF 为开发身份独立目录。
- capability 仅为 `core:default`，Rust 未注册 command、IPC handler 或 plugin；非空 CSP 生效。
- Web 与 Desktop 分别输出 `dist/`、`dist-desktop/`；Desktop 使用相对资源、HashRouter、无 PWA/Service Worker/Workbox，也不包含 Google Fonts 外链。
- 注册受限 Tauri RuntimeAdapter：安全空读和诊断可用；AI、文件、剪贴板、外链、持久化申请、Gist、更新安装等尚未实现的原生副作用统一 typed fail-closed。
- 新增桌面开发/构建/静态契约脚本、6 个 Tauri adapter 契约测试，以及 Windows 图标资源。
- ESLint 排除 Desktop/Rust 生成物，避免把构建产物误当源码扫描。

## 可执行证据

`cargo tauri build --no-bundle` 已完整执行 `beforeBuildCommand`、Desktop Vite 构建和 Rust release 构建，生成：

- 路径：`src-tauri/target/release/storyforge-desktop.exe`
- 大小：9,940,480 bytes
- SHA-256：`FC6B398A2EE580F09F56E1DB1CD06AA8D732821637E1C79EBAE0CFAA889E5320`

真实 WebView2 冒烟使用动态 CDP 端口和一次性隔离 profile，观察到：

- URL：`http://tauri.localhost/`，不是 `127.0.0.1:1420`；
- `document.readyState=complete`，React 根节点有 2 个子节点，首页 DOM 长度 6247；
- 页面包含“欢迎来到 StoryForge”“新建项目”等真实 UI，并完成截图人工核对；
- 未出现 JavaScript exception 或 CSP security error；唯一 warning 是 D1.1 预期的 durability 原生能力尚未启用；
- 最终根 PID 已退出，动态 CDP 端口已关闭，没有遗留客户端、WebView2、Vite 或 Node 长驻进程。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| Tauri adapter 定向测试 | PASS：3 files / 30 tests |
| 全量 Vitest | 513/514 PASS；唯一失败是既有 `character.dimension` AI manual 漂移，与本任务无关 |
| `npm run desktop:ui:build` | PASS：3430 modules；独立 `dist-desktop/` |
| `npm run build` | PASS：3430 modules；PWA 106 precache entries，Web 产物保持 |
| Desktop shell/build contract | PASS：Web 109 files，Desktop 106 files；Desktop 无 PWA/Workbox/Google Fonts，含 settings lazy chunk、CSS、pdf.js worker |
| required tables / architecture / D0.4 baseline / fixture artifacts | PASS：42 tables；三注册表守卫通过；夹具可重复 |
| ESLint | PASS：0 errors；33 个仓库既有 warnings |
| `cargo fmt --check` | PASS |
| `cargo clippy --all-targets -- -D warnings` | PASS |
| `cargo test` | PASS：lib/main/doc tests 均通过 |
| `cargo tauri build --no-bundle` | PASS：正式构建入口可重复执行 |

## 尚未完成 / 不得越界

- D0.2 独立审查、D0.4 完整动态基线和 D0.5 动作级功能基线仍是 D1.1 最终 PASS 的硬依赖。
- D1.2 仍需本地打包字体，以及首页/设置页/项目页直接启动与重启的完整路由 smoke。
- D1.3 尚未验证 Dexie/WebView2 重启持久化、开发/正式 profile 隔离、升级/卸载保留和失败注入。
- D1.4 尚未实现任何原生 IPC 最短闭环；当前 fail-closed 是安全边界，不是功能等价。
- 未生成 NSIS 安装包、未创建 `CN=StoryForge Self-Use` 证书、未签名，也未执行真实浏览器数据迁移。
- 全量测试中的 AI manual 漂移属于进入最终合并闸门前必须处理的仓库基线问题，但本次不混入无关生成文件。
