# D0.2 · 应用身份与支持范围独立审查

> 审查日期：2026-07-15
> 审查者：Codex（作者指定的独立只读审查；不冒充 Claude）
> 审查基线：`f257e92114afd1eb3384f7736dc01a1dd670ae0e`
> D0.2 source tip：`dd151db0c31b9c09e898c897870ec01fdffd9a08`
> 结论：**APPROVE**

## 1. 审查范围与提交关系

审查使用基于指定基线的新独立 checkout，未修改或依赖旧 `storyforge-desktop-d02` 工作树，也未读取未提交的 D1.3 本地证据。

`dd151db` 不是 `f257e92` 的后代。D0.2 源分支累计范围为：

```text
75f7da0 → 0506845  docs(desktop): freeze D0.2 technical identity
         → dd151db docs: freeze self-use signing boundary
```

基线已包含上述 D0.2 内容的等价重写提交并继续加入 D1.1/D1.2/D1.3 状态记录。因此审查同时核对源分支累计 diff 与基线中的集成快照，未把分叉后的其他任务误算为 D0.2。

完整阅读范围：`CLAUDE.md`、`AGENTS.md`、`COLLAB-WORKFLOW.md`、`MASTER-BLUEPRINT.md` §17、专项计划 D0.2/D1.3、应用身份决议、D1.1/D1.2 状态、dev/stable Tauri 配置、capability、`package.json`、PWA manifest 和 README 品牌事实。

## 2. 结论与 findings

- P0：无；
- P1：无；
- P2：无；
- P3：无。

D0.2 静态决议满足完成判据，可以标记 `PASS`。

## 3. 静态完成判据

| 判据 | 结果 |
| --- | --- |
| package/PWA/README 品牌与仓库 namespace 一致 | PASS |
| dev/stable `productName`、displayName、窗口标题、identifier、label、`dataDirectory` 唯一且相互一致 | PASS |
| dev/stable identifier 不同；self-use/beta/stable 不建立第二正式数据身份 | PASS |
| stable `devtools=false`，默认构建/启动不存在 CDP 或调试 profile 注入 | PASS |
| Windows 10 build 19045 兼容验证边界、受支持 Windows 11 x64 正式支持和首版排除项完整 | PASS |
| UDF 不直接复制/修改，升级、误切身份、回滚和 receipt 规则完整 | PASS |
| `CN=StoryForge Self-Use` 仅作者机器信任，私钥不导出，未配 updater 签名不得自动更新，self-use artifact 不得进入 beta/stable | PASS |
| 正式 publisher/受信任证书 Subject 未设占位或猜测，并明确延后 D5.2 | PASS |

静态命令：

```text
npm.cmd run check:desktop-shell
```

结果：`PASS`。

## 4. 合法延后项

### D1.3

- dev/stable 双向哨兵隔离；
- 同正式 identifier 覆盖升级后 IndexedDB、非密 localStorage 和 WebView 权限保留；
- 测试 identifier/`dataDirectory` 空 profile 与恢复原身份；
- 卸载/重装默认保留正式 UDF；
- UDF 不可写、磁盘不足、WebView2 缺失时安全失败且不激活半初始化库；
- 脱敏诊断摘要；
- 章节自动保存、关联数据和 schema upgrade 等 D1.3 自身持久化矩阵。

上述运行验证未执行不构成 D0.2 静态失败；任一验证失败会立即重开 D0.2，并阻止 G1 和真实数据迁移。

### D4.1 / D5.2

- D4.1 才实际生成和验证 self-use 本机自签名候选；
- D5.2 冻结正式受信任证书完整 Subject、installer publisher，并完成 Authenticode/Tauri Updater 双签名、可信时间戳和验证。

## 5. 放行范围

本次 `APPROVE` 只表示 D0.2 静态应用身份与支持范围决议闭环。它不等于 D1.3 PASS，不等于 G1 Go，也不表示 Windows 客户端已经可用、可导入真实数据、可作者自用、可封测或可公开发布。
