# D1.2 · 双构建与路由交付状态

状态：**IN PROGRESS — 受限实现已完成，最终 PASS 等待 D1.1 硬依赖闭环**

分支：`refactor/phase-desktop-task-d1-1`

日期：2026-07-15

范围：仅构建边界、本地字体、首页/设置页/项目页和一次性合成数据；没有连接真实密钥、默认正式 profile 或真实用户数据。

## 本次结论

D1.2 规定的 Web/Desktop 单一 runtime target 分流已经落地。Web 继续使用 `/storyforge/`、BrowserRouter 和 PWA；Desktop 使用相对资源、HashRouter、独立输出目录，不生成 manifest、Service Worker 或 Workbox。原 Google Fonts 网络依赖已替换为随产物分发的本地字体。

真实 release exe 已完成两次启动：第一次创建合成项目，第二次复用同一个一次性隔离 profile。首页、设置页和项目页在两次进程中都能直接刷新且不白屏，合成项目在重启后仍可见。该窄证据支持继续施工 D1.3，但不等于 D1.3 全量持久化/身份矩阵已经完成。

## 四问

1. **读什么**：只读取构建 target、路由状态、字体加载状态和合成项目；没有新增 AI 上下文，也没有绕过 `CONTEXT_SOURCES/assembleContext`。
2. **写什么**：只新增本地字体、构建契约、烟测脚本和一次性合成项目；没有新增 AI 业务写入，也没有绕过 `FIELD_REGISTRY + AdoptionSchema/adopt`。
3. **涉及哪些表生命周期**：没有修改 Dexie schema、迁移和表清单；合成项目仍通过现有应用 UI 和 store 写入，`PROJECT_TABLES` 保持唯一事实源。
4. **注册表是否缺登记**：没有新增表、字段、AI source 或 AI action，三个注册表无需修改。

## 已落地

- Web：`VITE_RUNTIME_TARGET=web`，`base=/storyforge/`，BrowserRouter，VitePWA/manifest/Service Worker 保留，输出 `dist/`。
- Desktop：`VITE_RUNTIME_TARGET=tauri`，`base=./`，HashRouter，禁用 VitePWA/Service Worker，输出 `dist-desktop/`。
- `index.html` 不再加载 `fonts.googleapis.com`；Inter、Source Serif 4 正常体/斜体、JetBrains Mono variable TTF 从 `src/assets/fonts/` 打包，两端共用；三份 OFL 许可证与源文件 SHA-256 随源码保留。
- 构建契约同时扫描 Web/Desktop 文本产物，拒绝远程 Google Fonts；验证两端四个字体资产、Web 预缓存、Desktop 无 `/storyforge/`、manifest、Service Worker、Workbox。
- 新增 `npm run check:desktop-routes`：Windows-only；动态分配 CDP 端口；每次运行使用唯一 profile；启动前拒绝复用同路径 exe；记录 PID/端口/profile 后才交互；只按记录 PID 树停止并验证端口关闭；最终删除隔离 profile。

## 构建产物

`npm run desktop:build:dev` 已完成 dev identity 的前端与 Rust release 构建。真实 smoke 通过 `WEBVIEW2_USER_DATA_FOLDER` 使用开发身份下的一次性隔离 profile；脚本还会读取内嵌前端入口并确认包含 `io.github.yuanbw2025.storyforge.dev`，从而拒绝把 CDP 烟测误用于 stable 构建。

- 路径：`src-tauri/target/release/storyforge-desktop.exe`
- 大小：11,358,720 bytes
- SHA-256：`1F152E4E2BAE28B991548314EE04483ED80D4F459CA4DC43ECE74CE9C08F2C97`

## 真实 WebView2 证据

| 项目 | 首次启动 | 重启 |
| --- | --- | --- |
| 根 PID | 2592968 | 2535764 |
| 动态 CDP 端口 | 57095 | 57131 |
| 首页 | `#/` PASS，并直接刷新 | `#/` PASS，并直接刷新 |
| 设置页 | `#/settings` PASS，并加载 lazy chunk | `#/settings` PASS，并直接刷新 |
| 项目页 | `#/workspace/1` PASS，并直接刷新 | `#/workspace/1` PASS，并直接刷新 |

附加结果：

- 合成项目 `D1.2 路由烟测 1784102233744` 在第二个进程中可见；
- 内嵌 bundle 的 dev identifier 检查通过；stable identity 没有参与本记录的 CDP 自动化；
- Inter、Source Serif 4 正常体/斜体、JetBrains Mono 均实际加载；
- manifest link、Service Worker registration、Cache Storage key、`/storyforge/`/`sw.js`/远程字体禁止请求、JavaScript exception、console error 均为 0；
- 两个 PID 树和两个动态端口均已关闭，一次性 profile 已删除；没有遗留 StoryForge、WebView2、Vite 或 Node 长驻进程。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| Web production build | PASS：3430 modules；113 files；PWA 110 precache entries，包含四个本地字体 |
| Desktop production build | PASS：3430 modules；110 files；独立 `dist-desktop/` |
| `npm run check:desktop-build-contract` | PASS：双产物路径、PWA/SW、字体、lazy chunk、CSS、pdf.js worker 契约全绿 |
| `npm run check:desktop-routes` | PASS：两次真实启动，三路由直接刷新，合成项目跨进程可见，PWA/请求/字体/异常边界全绿 |
| required tables / architecture / shell contract | PASS：42 tables；三个注册表守卫和 D1.1 shell 契约通过 |
| ESLint | PASS：0 errors；33 个仓库既有 warnings；本任务没有新增 warning |
| 全量 Vitest | 513/514 PASS；唯一失败为既有 `character.dimension` AI manual 漂移 |
| `cargo fmt --check` | PASS |
| `cargo clippy --all-targets -- -D warnings` | PASS |
| `cargo test` | PASS：lib/main/doc tests |
| `npm run desktop:build:dev` | PASS：dev identity 前端 build + Rust release build |

## 尚未完成 / 不得越界

- D1.1/D0 总门未闭环，因此 D1.2 不标最终 `PASS`。
- D0.4 生产 `web-tab` 动态基线和 D0.5 动作级功能清单尚未完成；本任务的 Web production build 契约不能替代它们。
- D1.3 仍需验证章节编辑/自动保存、更多关联表、schema upgrade、dev/stable UDF 隔离、同身份覆盖升级、卸载/重装保留和 UDF 不可写/磁盘不足/WebView2 缺失失败注入。
- D1.4 仍未实现原生 IPC、流式 AI 和文件最短闭环。
- 未使用真实密钥、真实浏览器数据或默认正式 UDF；没有生成安装包、证书或签名。
