# D1.3 · Dexie / WebView2 持久化进度

状态：**IN PROGRESS — dev identity 优先持久化链路已通过，完整矩阵仍在施工**

分支：`refactor/phase-desktop-task-d1-1`

施工基线：`79778f8`

日期：2026-07-15

范围：只使用 dev identity、一次性隔离 WebView2 profile 和合成项目/章节；不读取或写入默认正式 UDF、真实浏览器数据、真实正文或密钥。

## 当前结论

`npm run check:desktop-persistence` 已在 2026-07-15 新构建的 dev-identity release exe 上通过。烟测只通过现有 UI/store 创建合成项目、卷、章节和正文，等待 1500 ms debounce 后读取普通 IndexedDB API 核验，再依次执行：正常关窗并重启原 profile、启动独立空 profile、恢复原 profile。项目、outline、章节正文、字数和非密 localStorage 哨兵均符合预期，且主键与父子/FK 关系在三次原 profile 启动间保持不变。

`npm run check:desktop-persistence-upgrade` 进一步构建同一 dev identity 的 3.7.5 / 3.7.6 两个真实 release artifact，把 baseline 暂存到一次性运行目录，在进程退出后以新二进制覆盖同一个 exe 路径，再沿用同一 primary profile 启动。项目、outline、章节正文、字数、主键和 localStorage 哨兵均保持，证明 dev 通道的同身份、不同版本/哈希、同路径覆盖升级链路成立。

启动数据门也已收紧：schema 检查、Dexie 打开和迁移收尾全部成功后，才允许 Prompt/Workflow seed 初始化；任一前置步骤 blocked/失败都会短路 seed 写入。真实 Dexie v28→v29、v30→v31、v31→v32、v33→v34、v34→v35 fixtures 和生产缺表不删库回归均通过。

这已经覆盖本轮优先要求和 dev 同身份覆盖升级，但不是 D1.3 的完整持久化矩阵。卸载/重装保留、stable identity 非 CDP 运行矩阵，以及 UDF 不可写、磁盘不足、WebView2 缺失等失败注入仍未完成，因此 D1.3 继续标为 `IN PROGRESS`。

## 已确认边界

- dev identifier：`io.github.yuanbw2025.storyforge.dev`；stable identifier：`io.github.yuanbw2025.storyforge`。
- 两个通道的主窗口均固定 `label=main`、`dataDirectory=webview-data`，但 app data 根目录必须由 identifier 隔离。
- 自动化/CDP 只允许 dev 构建；烟测必须验证内嵌 bundle 含 dev identifier。stable 默认 UDF 不得被 `WEBVIEW2_USER_DATA_FOLDER` 或远程调试参数覆盖。
- Dexie 数据库名为 `storyforge`；项目、outline 和章节分别使用 `projects`、`outlineNodes`、`chapters` store。
- `ChapterEditor` 的正文变更通过 `useAutoSave` 以 1500 ms debounce 写回 `chapters.content/wordCount`；组件卸载时对脏数据执行 flush。
- D1.3 不修改 Dexie schema、不引入 SQLite、不直接复制或修改 WebView2 UDF 文件。

## 四问

1. **读什么**：只读 identity 配置、Dexie schema、项目/outline/章节 store 和自动保存实现；没有新增 AI 上下文。
2. **写什么**：只通过现有应用 UI/store 写入合成项目、卷、章节与正文，并写入一个明确命名的非密测试哨兵；没有 AI 业务写入。
3. **涉及哪些表生命周期**：不增删表、不改 schema；运行验证涉及现有 `projects`、`outlineNodes`、`chapters`，生命周期仍由 `PROJECT_TABLES` 与现有 store 管理。
4. **注册表是否缺登记**：没有新增表、字段、AI source 或 AI action，三个注册表无需修改。

## 验证矩阵

| 场景 | 当前状态 | 完成要求 |
| --- | --- | --- |
| 合成项目跨进程重启 | PASS（dev 隔离 profile） | 正常关闭首进程后，第二进程沿用同一 profile 打开同一项目 |
| outline + 章节记录创建 | PASS（dev 隔离 profile） | 合成卷/章节落入既有 store；项目/卷/outline/章节 ID 为 `1/1/2/1`，父子与 project FK 一致 |
| RichEditor 正文自动保存 | PASS（dev 隔离 profile） | debounce 后正文与正数字数写入 `chapters`，重启后 UI 与 IndexedDB 一致 |
| 非密 localStorage | PASS（dev 隔离 profile） | 同 profile 重启保持，独立 profile 不可见，恢复原 profile 后重新出现 |
| 独立空 profile | PASS（dev 隔离 profile） | 第二个全新 dev profile 看不到原合成项目、outline、章节或哨兵 |
| 恢复原 profile | PASS（dev 隔离 profile） | 切回原 profile 后项目、章节、正文、字数和哨兵全部恢复 |
| schema / required tables | PASS（当前 dev artifact） | 运行态 IndexedDB version `370`、42 个 object stores；`check:required-tables` 的 42 表契约通过 |
| schema upgrade fixtures | PASS（合成内存库） | v28→v29、v30→v31、v31→v32、v33→v34、v34→v35 真实 Dexie transitions 保留/转换/删表语义符合契约 |
| 启动 seed 顺序 | PASS（定向回归） | schema → Dexie open → migration finalize 后才执行 Prompt/Workflow seed；blocked/open/finalize 失败均短路 seed writer |
| 同身份覆盖升级 | PASS（dev 隔离 profile） | 3.7.5 与 3.7.6 的 identifier 相同、SHA/版本资源不同；新二进制覆盖同一临时 exe 路径后沿用同一 profile，全部合成数据不变 |
| 卸载/重装保留 | 待 D4/D5 安装包 | 默认不删除正式 UDF；安装器证据形成前不得宣称完成 |
| UDF 不可写 / 磁盘不足 / WebView2 缺失 | 待设计与注入 | 明确失败，不创建用户可见半初始化库，不清库 |

## 可重复验证与最近证据

- 入口：`npm run check:desktop-persistence` 和 `npm run check:desktop-persistence-upgrade`。升级编排只改变 patch version 的临时 Tauri overlay，生成同 identifier 升级 artifact；路由烟测在启动前扫描两个 exe 的固定 dev identifier，任一不是 `io.github.yuanbw2025.storyforge.dev` 都立即拒绝运行，stable artifact 不会获得 CDP 或 profile 覆盖参数。
- baseline artifact：`3.7.5`，11,358,720 bytes，SHA-256 `945D0FA7609C49D03EFA07D21851150C59DE4FD9B0FE08BB828FC3C9A2449B98`；upgrade artifact：`3.7.6`，11,358,720 bytes，SHA-256 `CAEFB6AF8A7EEEC260F85903F0A40351DC6669C3F7F8EA6C2C6AD4109F685027`。两者的文件/产品版本和 SHA 均不同，身份相同。
- 最近一次五次启动分别使用动态端口 `64385/64416/64430/64484/64516`，记录根 PID `536668/539252/539020/538368/537924`；五次均由 `CloseMainWindow` 正常退出，无强制回退。第 5 次启动前，脚本在进程已退出且精确 PID 检查为空时，将同一路径的 baseline exe 覆盖为 upgrade exe。报告生成前已确认所有记录进程退出、端口关闭；脚本退出收尾已删除本次临时 primary/isolated profile、运行目录和构建编排目录，并恢复正常 release artifact。
- 运行结果：`autosavePersisted=true`、`restartPersisted=true`、`isolatedProfileWasEmpty=true`、`primaryProfileRestored=true`、`localStorageSentinelPersisted=true`、`sameIdentityOverwriteUpgradePersisted=true`；JS exception、console error、manifest/Service Worker/cache 禁止请求均为 0，四个打包字体面全部 loaded。
- 烟测不读取 WebView2 UDF 文件；所有数据核验都在受控页面内通过普通 IndexedDB/localStorage API 完成。独立性通过新建空 profile 和恢复原 profile 的行为验证，不复制、改写或枚举 UDF 内容。
- 首次扩展运行暴露了一个有效失败信号：`taskkill /F` 后 localStorage 哨兵未能跨进程保持。最终主链路改为正常关窗，并仅在超时后对记录 PID 使用强制回退。异常崩溃/断电下的 durability 仍属于待设计失败注入，不能由本次正常关闭 PASS 推断。
- schema/启动顺序回归入口：`npm test -- tests/regression/R-desktop-bootstrap-order.test.ts tests/regression/R-db-upgrade-fixtures.test.ts tests/regression/R-17-ensure-schema.test.ts`；3 files / 10 tests 全绿。顺序门实现位于 `src/lib/db/bootstrap.ts`，不改变 Dexie schema、seed 内容或三个注册表。

## 本轮质量闸门

- PASS：`node --check scripts/windows-desktop-route-smoke.mjs`、`node --check scripts/windows-desktop-persistence-upgrade-smoke.mjs`、`check:desktop-shell`、`check:required-tables`、`check:architecture`、`tsc --noEmit`、lint（0 errors，33 个既有 warnings）、Rust fmt/clippy/test。
- PASS：定向 3 files / 10 tests、`desktop:build:dev`、`check:desktop-build-contract`、新 artifacts 的 `check:desktop-persistence-upgrade`、Web production build。
- 全量 Vitest 为 113 files PASS / 1 file FAIL、516 tests PASS / 1 FAIL；唯一失败仍是既有 `tests/registry/ai-manual.test.ts` 检测到生成版 AI manual 漂移，与本轮 D1.3 代码无关。本轮不越界重生成 AI 文档，也不把全量测试写成全绿。

## 尚未完成 / 不得越界

- D1.3 当前为 `IN PROGRESS`；本轮 dev-only 优先链路和同 dev identity 覆盖升级通过不等于完整身份、安装和失败矩阵通过，不能标 `PASS`。
- stable 默认 UDF 尚未创建或打开；真实 profile 验证不得借 CDP 完成。
- 没有执行安装器覆盖安装、卸载/重装或磁盘/UDF/WebView2 失败注入；本轮只验证了临时路径中的 exe 文件覆盖。
- 没有读取、复制、移动或修改 Chrome、Edge 或作者真实 WebView2 UDF。
- D0.2、D0.4、D0.5 和 D1.1/D1.2 的正式状态门仍未闭环，G1/G2 均未通过。
